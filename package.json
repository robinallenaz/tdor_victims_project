{
  "name": "tdor-vigil-collage",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:photos": "node scripts/build-photos-json.mjs",
    "prestart": "npm run build:photos",
    "start": "http-server . -p 8080 -c-1",
    "predev": "npm run build:photos",
    "dev": "live-server --port=8080 --wait=200 --open=/index.html"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  }
}
